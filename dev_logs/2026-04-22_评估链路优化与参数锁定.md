# 2026-04-22｜评估链路优化：JSON 容错、参数锁定与可回答性过滤

> 优化时间：2026-04-22 16:18
> 目标：解决测试集 JSON 解析失败、评估覆盖率停滞问题，锁定关键参数，建立标准化评估流程。
>
> **重要：本文档包含已锁定的关键参数，请勿将其喂给 AI 要求自动修改代码，以免参数被误改导致指标回退。**

---

## 一、评估现状与核心问题

### 1.1 优化前指标

| 指标 | 优化前 | 当前值 | 目标 |
|------|--------|--------|------|
| 要点覆盖率 | 9.4% | **25.0%** | 40%+ |
| 平均相关性 | 3.6/5 | **4.0/5** | 4.0+ |
| JSON 解析成功率 | ~80% | **100%** | 100% |

### 1.2 核心矛盾

- **相关性高（4.0/5）** → 检索到的文档主题正确
- **覆盖率低（25.0%）** → 答案要点未包含在检索到的 chunk 中
- **部分测试条目本身在文档中无法找到答案** → LLM 生成测试集时过度推断，导致评估被噪声污染

---

## 二、已锁定参数（不可回退）

| 参数 | 锁定值 | 位置 | 说明 |
|------|--------|------|------|
| `vec_k` / `bm25_k` | 20 | `app.py`, `evaluate_batch.py` | 粗排候选池大小 |
| `rerank_k` | 5 | `app.py` | CrossEncoder 精排后保留数 |
| `num_ctx` | 8192 | `ollama_generate` | LLM 上下文长度 |
| `chunk_size` | 1500 | `split_by_headings` | 结构化切分最大长度 |
| `chunk_overlap` | 200 | `split_by_headings` | 相邻 chunk 重叠 |
| `BATCH_SIZE`（生成） | 3 | `generate_test_set_batch.py` | 生成批大小 |
| `EVAL_BATCH_SIZE` | 5 | `evaluate_batch.py` | 评估批大小 |

---

## 三、已完成的修复

### 3.1 JSON 解析容错（`generate_test_set_batch.py`）

| 项目 | 内容 |
|------|------|
| **现象** | `list indices must be integers` 或 `Extra data` 错误 |
| **根因** | LLM 返回的 JSON 可能被包裹在 `{"qa_pairs": [...]}` 中，或包含多个片段 |
| **修复** | 增加正则提取完整 JSON 数组或对象；自动提取 `qa_pairs` 字段；失败时打印响应片段便于调试 |
| **效果** | 生成成功率从 ~80% 提升至 **100%** |

### 3.2 检索参数对齐（`evaluate_batch.py`）

| 项目 | 内容 |
|------|------|
| **问题** | 评估时检索 `k=3`（纯向量），与前端 Hybrid 粗排不一致 |
| **修复** | 评估时检索 `k=20`，与前端 Hybrid 粗排一致；上下文窗口扩大至 8192 |
| **效果** | 相关性稳定在 4.0，检索延迟正常 |

### 3.3 BM25 分词优化（`HybridRetriever`）

| 项目 | 内容 |
|------|------|
| **问题** | 英文缩写被 jieba 拆开（如 `PPO` → `P`、`P`、`O`） |
| **修复** | 保留完整英文缩写；中文使用 `jieba.cut`，英文数字按空格分词 |
| **效果** | 术语精确召回率提升（PPO、GRPO 等可命中） |

---

## 四、当前瓶颈与解决方案

### 4.1 瓶颈：测试集中的"不可回答"条目

LLM 生成测试集时可能产生文档中不存在的细节问题，导致评估时覆盖率被系统性压低。

### 4.2 解决方案：可回答性过滤

在生成测试集后增加过滤，剔除无法在源文档中找到至少一个要点的条目。

**实施步骤**：

```bash
# 1. 生成原始测试集
python generate_test_set_batch.py

# 2. 清洗不可回答条目
python filter_test_set.py

# 3. 运行评估（自动读取 test_set_clean.json）
python evaluate_batch.py
```

### 4.3 清洗脚本 `filter_test_set.py`（新增）

```python
import json
from pathlib import Path

def filter_unanswerable(test_set, docs_dir="docs", min_matches=1):
    """
    过滤掉在源文档中无法找到至少 min_matches 个要点的测试条目。
    """
    filtered = []
    for item in test_set:
        source = item.get("source")
        if not source:
            continue
        doc_path = Path(docs_dir) / source
        if not doc_path.exists():
            continue
        text = doc_path.read_text(encoding="utf-8").lower()
        points = item.get("answer_points", [])
        matched = sum(1 for p in points if p.lower() in text)
        if matched >= min_matches:
            filtered.append(item)
        else:
            print(f"丢弃不可回答: {item['question'][:50]}... (匹配 {matched}/{len(points)})")
    return filtered

if __name__ == "__main__":
    with open("test_set.json", "r", encoding="utf-8") as f:
        test_set = json.load(f)
    cleaned = filter_unanswerable(test_set)
    with open("test_set_clean.json", "w", encoding="utf-8") as f:
        json.dump(cleaned, f, indent=2, ensure_ascii=False)
    print(f"清洗完成：{len(cleaned)}/{len(test_set)} 条保留")
```

### 4.4 预期收益

| 指标 | 过滤前 | 过滤后（预期） |
|------|--------|---------------|
| 要点覆盖率 | 25.0% | **35%~45%** |
| 准确性 | 偏低（噪声拉低） | 同步提升 |
| 完整性 | 偏低 | 同步提升 |

---

## 五、标准化评估流程

```bash
# 步骤 1：生成原始测试集
python generate_test_set_batch.py
# 输出：test_set.json

# 步骤 2：清洗不可回答条目
python filter_test_set.py
# 输出：test_set_clean.json
# 修改 evaluate_batch.py 读取 test_set_clean.json

# 步骤 3：运行评估
python evaluate_batch.py
# 输出：evaluation_report.json
```

---

## 六、避坑指南（重要）

| 注意事项 | 说明 |
|----------|------|
| **禁止将本文档直接喂给 AI 要求"按此修改代码"** | AI 可能误将 `chunk_size` 改回 500 或 `vec_k` 改回 10，导致指标回退 |
| 手动修改参数 | 若需调整，请手动修改对应文件中的常量，并确保 `app.py` 与 `evaluate_batch.py` 保持一致 |
| 温度监控 | 依赖 `osx-cpu-temp`，若未安装可忽略警告或注释相关代码 |
| 并发风险 | 跑 batch 时不要同时在网页端提问，Ollama 可能出现超时/截断 |

---

## 七、后续优化方向

| 方向 | 预期效果 | 复杂度 |
|------|----------|--------|
| 增大 `chunk_size` 至 2000~2500 | 减少答案被切散，提升覆盖率 | 需重建索引 |
| 引入 `CrossEncoder` 精排后再截断 | 确保最相关片段进入 LLM | 已实现 |
| 测试集生成时增加"可回答性"提示词约束 | 从源头减少无效条目 | 中 |

---

**文档结束**
