# 0806强化学习3蒙特卡洛方法

## 蒙特卡洛方法​

## 之前介绍的通过迭代法解决bellman方程的过程中，都假设模型已知，也就是环境的动态属性已知（

## ​
是随环境变化，之前都假设知道每个时刻的​
，并且状态数量少且是离散

的）。然而在实际问题中，我们可能对环境的动态特性并不是那么清楚，但是我们可以得到足够多的

数据，那么我们同样可以用强化学习来建模解决这个问题，这类不利用模型的算法被称为Model-free

的方法。Monte Carlo方法便是一种Model-free的方法。​

## p(s , r∣s, a)
′
p(s , r∣s, a)
′

Monte Carlo的核心思想是用样本均值来近似期望。依据大数定理，对于随机变量X，给定N个iid采样

的样本​
, 则​
可以视为​
的无偏估计。​
{x
​}
​
j
j=1
N
=
xˉ
​
​x
​
N

j=1
∑

j
E(X)

Monte Carlo Method​

回忆策略评估和策略优化的过程中，需要计算​
和​
，但是在Model-free的算法中不知道精确

的p和r，可以采用Monte Carlo Method直接估计给定策略下的v(s) 和q(s, a)，我们先讨论v(s)，q(s, a)

与之类似。

p
​(s ∣s)
π
′
r
​π

策略评估：​
​
v
​ =
π
r
​ +
π
γP
​v
​
π

策略优化：​
​
π =
​(r
​ +
π
arg max
π
γP
​v
​)
π

回顾v(s)的定义：每个状态下的累计回报的期望​

​
v
​(s) =
π
E[G
​∣S
​ =
t
s]

由大数定理可知，可以用样本均值估计期望，假设​
是多个episode得到的n个s状态的累计回

报，则​
是​
的无偏估计。​

g
​, ..., g
​
1
n

v(s) =
​
n
g
​ + ... + g
​
1
n
G
​t

假设某个episode为​
，针对出现了两次的​
,有两种方法：​
S
​, A
​, R
​, A
​, S
​, R
​, S
​, A
​, R
​
1
2
3
1
4
S
​1

First-visit： 每个episode的数据只记录每个S第一次出现的值，即只记录

​
g(S
​) =
1
R
​ +
4
γR
​ +
3
γ R
​
2

Every-visit: 每个episode的数据记录每次S出现的值，还记录​
​
g(S
​) =
1
R
​4

下面的伪代码是Every-visit的，采取倒序遍历所有episode，计算得到每一个时刻的奖励值，分别放到

对应状态的集合中，计算每一个状态的累计回报来估计真实的v(s)。被访问次数多的状态计算出的v(s)

越接近真实。q(s, a)的计算与之类似。​

Monte Carlo的策略优化与迭代法一致。策略评估和策略优化依次进行，即针对每个episode，先进行

策略评估，再进行策略优化。但是策略优化面临一个问题，因为采取贪心的策略，要确保所有(s,a)都

被计算到，意味着我们需要从每个(s,a)对开始生成足够多的episode。​

为了解决以上问题，采用soft policy ε-greedy，足够长的episode中可能就会包含每个(s,a)对足够多

次，不需要从每个(s, a)对开始采集大量episodes，具体公式如下：

其中​
是状态总数。该式保证了采取贪婪行动的概率总是大于其他行动，通过调整​

的大小，可以实现exploitation & exploration的平衡。

ϵ ∈[0, 1], ∣A(s)∣
ϵ

当状态的数量很大时，Monte Carlo的计算量要显著低于迭代法，因为可以只对重要的(也就是在

episode中经常被访问到的)状态进行采样和更新，而求解Bellman方程需要更新所有的状态。​

存在的问题：

每次更新需要等到一个episode结束，只适合Episodic Task。​

当episode很长或者数量很多时，效率低​

Temporal Difference 时序差分​

可以在一边生成episode的过程中，一边用生成好的样本进行估算，不需要完整的episode。​

按照Robbins-Monro算法（一种随机近似算法），​
作为要更新迭代的值，​
是采样到的样本

值，就有

v(S
​)
t
G
​t

​
v(S
​) =
t
v(S
​) +
t
α[G
​ −
t
v(S
​)]
t

其中​
，又已知：​
G
​ =
t
R
​ +
t+1
γR
​ +
t+2
... = R
​ +
t+1
γG
​
t+1

​
v
​(s) =
π
E[G
​∣S
​ =
t
s] = E[R
​ +
t+1
γG
​∣S
​ =
t+1
t
s] = R
​ +
t+1
γv
​(S
​)
π
t+1

用​
近似代替​
,可以得到：​
E(G
​)
t
G
​t

​
v(S
​) =
t
v(S
​) +
t
α[R
​ +
t+1
γv(S
​) −
t+1
v(S
​)]
t

这样只需要知道下一个状态就可以对当前状态进行更新.​

与之类似的，在策略优化时需要用到的q(s, a)也可以使用TD进行更新。在这里S'是下一个状态，A'是根

据策略在S'下采取的动作，区别是如果S'是终止状态，没有A'，额外定义q(S', A') = 0。当更新了q(s, a)

后，可以再对策略进行更新（ε-greedy），​