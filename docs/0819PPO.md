# 0819PPO

## PPO​

## 传统强化学习都是在维护表格，也就是要列举出在所有状态下应该采取的行动。这面临两个问题。一

## 是当状态太多时，需要大量样本，并且存储表格状态也需要大量空间；二是只适合状态连续的场景，

对于温度、GDP等指标则需要离散化成不同的状态（单个指标离散化会造成一定的精度损失，当多个

指标需要离散化时会加重这种损失）。

更好的方法是构造方程来预测给定状态下的v和q，也就是构造​
，训练目标就是

​
， ​
表示被访问的概率。如果采用梯度下降，则更新公

式为：

(s, w) ≈
v^
v
​(s)
π
min l(w) =
​μ(s)[v
​(s) −

s
∑
π
(s, w)]
v^
2
μ(s)

​
w = w −η∇l(w) = w + η∇
​μ(s) ⋅

s
∑
2[v
​(s) −
π
(s, w)]∇(s, w)
v^

又已知​
，只需要知道下一

个状态就可以对当前状态进行的w进行更新.​

v
​(s) =
π
E[G
​∣S
​ =
t
s] = E[R
​ +
t+1
γG
​∣S
​ =
t+1
t
s] = R
​ +
t+1
γv (S
​)
π
t+1

PPO前身Policy gradient​

之前介绍对于continuing task，有不考虑衰减的奖励
，考虑衰减的奖励

​
。此外，还有differential return，其中​
是基于当前所有奖励算出的平均

值，这样计算的好处是能减少方差。

G
​ =
t

​R
​

k=t+1

G
​ =
t

​γ
R
​

k=t+1

k−t
k
r(π)

利用differential return，可以改写基于TD和梯度下降的Sarsa，​
是一个回归模型，拟合真

实的q值。​

## ​(S, A, w)
q^

之前介绍的policy iteration是首先计算state-value值，再采用贪心的策略更新策略。现在想直接对策

略进行优化，表示为​
，其满足​
。​
π(a∣s, θ)
π(a∣s, θ) ≥0,
​π(a∣s, θ) =

a∈A
∑
1

首先想到可以用softmax函数，其中h函数可以用深度学习的回归模型来实现​

​
π(a∣s, θ) =
​

​ e
∑b∈A

h(s,b,θ)
eh(s,a,θ)

训练目标：

​
J(θ) =
​d (s)v (s) =

s∈S
∑
π

​d (s)
​π
​(a∣s)q (s, a)

s∈S
∑
π

a∈A
∑
θ
π

其中​
表示状态s被访问的概率，目标是最大化​
，也就是希望访问的都是价值高的state，这

里的v就是state-value，​
。需要计算的也就是​
​

d (s)
π
J(θ)
v
​(S) =
π
E[G
​∣S
​ =
t
s]
∇
​J(θ)
θ

​
θ = θ + η∇
​J(θ)
θ

依照policy gradient theorem（这个证明比较复杂，这里不写了）：​

## ∇
​J(θ) =
θ
∇
​[
​d (s)
​π
​(a∣s)q (s, a)] ∝
θ

## ​d (s)
​π
​(a∣s)q (s, a)
​ =

## s∈S
∑
π

## a∈A
∑
θ
π

​(a∣s)
θ
∇
​π
​(a∣s)
θ
E
​q (s, a)∇
​lnπ
​(a∣s)
s∈S,a∈π
​(a∣s)
θ

## π
θ

## 下图中用G近似替代了q，都是奖励函数。​

面临问题:episode怎么生成，可能很长，episode生成和策略更新不可同时进行。​

Off-policy policy gradient​

之前介绍的是on-policy的，即采样和更新都是对同一个​
进行，因此只能能同时做一件事。off-

policy值利用参考的相似的​
生成的episode更新目标的​
。​

θ

采用Off-policy policy gradient，对应的梯度加一个距离系数，变成了：​

​
∇
​J(θ) =
θ
E
​[
​q (s, a)∇
​ln(a∣s)]
β β(a∣s)

π (a∣s)
θ
π
θ

在PPO中，用​
作为​
。此外用Advantage function ​
代替q，因为

​
,相当于减去平均值，使得方差更小更平滑，就得到了TRPO​

πt−1
β
A (s, a) =
π
q (s, a) −
π
v (s)
π

v(s) =
​π(a∣s)q(s, a)

a∈A
∑

面临问题：存在限制条件，要求参考策略和目标策略距离接近，难以优化。PPO消除了限制条件​

PPO​

为了消除限制条件，首先定义​
，对​
进行clip操作。实验证明，只对上区间做

clip效果最好。​

r(θ) =
​
π
​(a∣s)
θ
​
old

π
​(a∣s)
θ
r(θ)

​
r(θ) = 1 + ϵ, if r(θ) > 1 + ϵ

红色项是因为采用了Advantage function（​
），需要一个模型​
来求出针

对当前状态下的状态值，这里与policy采用同一个网络参数,因此放在一起优化，红色项最小化预测​

与真实​
之间的距离；第三项为了exploration，采用熵的形式，提升模型可探索域的大小。​

A(s, a) = q(s, a) −v(s)
V
​θ

V
​θ
V
​
target

整体的训练流程就是基于​
生成一个episode，利用episode计算目标函数中的值，更新​
.伪代码中

​
一般与​
用同一个模型。​

θ
​
old
θ
ϕ
​0
θ
​0