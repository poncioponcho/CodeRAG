# 0723强化学习2-策略迭代

## 强化学习2-policy iteration​

前边的笔记介绍了Bellman方程，提到求解Bellman方程一般采用policy iteration，今天就介绍怎么用

## 迭代法求解Bellman方程的最优解。​

Bellman Optimal Equation,BOE​

最优策略，顾名思义，就是在任何state下的state value都大于其他策略的state value。​

最优策略条件下的bellman方程：​

​
v(s) =
​
​π(a∣s)[
​
​p(s , r∣s, a)[r +
π
max

a∈A
∑

s′
∑

r
∑
′
γv(s )]] =
′

​
​π(a∣s)q(s, a), s ∈
π
max

a∈A
∑
S

上式存在两个未知数v和​
。考虑到有​
, 可以先固定v，先对​
求max，接下来就只用

求解v了：​

​π(a∣s) =

a
∑
1
π

​
v(s) =
​  q(s, a)
a∈A
max

接下来的对v的求解和证明参考论文：Mathematical Foundations of Reinforcement Learning​

首先定义压缩映射的概念，

而BOE的矩阵形式​
就是一个压缩映射，对于压缩映射，我们就有：​
v =
​(r
​ +
π
max
π
γP
​v）
π

也就是说对于BOE，存在且唯一存在一个最优的state value ​
，可以通过迭代的方式

​
收敛到​
，无论给定任何的初始状态​
，其收敛速度由​
决定。​

v∗

v
​ =
k+1

​(r
​ +
π
max
π
γP
​v
​）
π
k
v∗
v
​0
γ

在每一次迭代过程中（下一章详细介绍具体的迭代过程），采用上边提到的​

。该公式右边也就是采用贪心策略，对每个状态采取能获得最大state value的行动​

v
​(s) =
k+1

​  q
​(s, a)
a∈A
max
k

其中​
。求解出​
之后自然的就能得到最优的
,可以证明这样得到的​
就是

最优策略。

a =
∗

​q
​(s, a)
a
arg max
k
v∗
π∗

​
π =
∗

​(r
​ +
π
arg max
π
γP
​v )
π
∗

Policy iteration​

给定一个初始策略​
, 分为两个步骤：​
π
​0

1. 策略评估：通过迭代法不断更新当前策略下的state value，直到state value的前后差值小于一个参

数（收敛到一个定值）：

​
v
​ =
π
r
​ +
π
γP
​v
​
π

注意这里的策略是固定的，由于采用贪心策略，所以在每个状态下采取的动作也是固定的，即

​
,而其他的动作都为0。因此在下面算法中省略了​
。​
π(a ∣s) =
∗
1

​π(a∣s)

a∈A
∑

2. 策略优化：基于更新后的​
更新策略：​
v
​π

​
π =
​(r
​ +
π
arg max
π
γP
​v
​)
π

具体到每一个状态：

​
π(s) =
​
​π(a∣s) ⋅
π
arg max

a
∑
​
​p(s , r∣s, a)[r +

s′
∑

r
∑
′
γv
​(s )] =
π
′

​
​π(a∣s) ⋅
π
arg max

a
∑
q
​(s, a)
π

​
a =
∗

## 整个流程直到策略不再改变停止，伪代码如下：

v和​
的更新是交替进行的，其中v的更新由于要进行多次迭代，速度会比较慢，可以通过适当放宽收

敛标准，减少迭代次数；而​
的更新直接贪心，速度比较快。​

举一个栗子，引用自西湖大学赵世钰老师的【强化学习的数学原理】：