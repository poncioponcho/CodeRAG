# 0709Flash attention-原理

## Flash attention-原理​

## 自注意力模块的计算和空间复杂度为​
，N为序列长度（假设​
，batch大小为

## b, 注意力头数为a，计算​
的计算复杂度为​
，用fp16存储​
的空间复杂度为​

）。这导致在处理长序列时速度变慢且内存需求巨大，也限制了模型输入输出的序列长度。

## O(N )
2
Q, K, V ∈RN×d

## QKT
2bN d
2
QKT
2bN a
2

SRAM的运算速度很快，但是空间远小于HBM显存。自注意力对应的空间需求巨大，对应的将数据从

HBM搬运到SRAM也消耗了大量时间。FlashAttention提出优化算法来提高注意力模块的计算速度。​

传统attention​

例如在GPT3中，一般N=2048，d=128。对于传统算法，S 和P 远大于Q K V O，在SRAM中放不下，

需要在HBM中进行存储，计算时需要反复访问HBM，搬运数据浪费了大量时间。​

​

S = QK
∈R
,
P = softmax(S) ∈R
,
O = PV ∈R
,
⊤
N×N
N×d

​
(1)

需要把输入​
从HBM中读取，并计算完毕后把输出​
写入到HBM中。​
Q, K, V
O

第一步把​
读取出来计算出​
，然后把​
存回去，内存访问复杂度​

Q, K
S = QK⊤
S
Θ Nd + N
(
2)

第二步把​
读取出来计算出​
，然后把​
存回去，内存访问复杂度​
。​
S
P = softmax(S)
P
Θ N
(
2)

## 第三步把​
读取出来计算出​
，然后计算出结果​
，内存访问复杂度​

综上所述，整体的内存访问复杂度为​
。总结一下标准注意力面临两个问题：​
Θ Nd + N
(
2)

## 1. 显存占用多，由于计算过程中存储完整注意力矩阵P和S，需要​
的空间。​
O(N )
2

## 2. HBM读写次数多，需要传输的数据量大。​

Flash attention​

将从输入的​
到输出​
的整个过程进行融合，省去​
矩阵的存储开销。此外，为了将计算

过程的结果完全存储在SRAM中，摆脱对HBM的依赖，采用分片（tiling）操作，每次进行部分计算，

确保这些计算结果能在SRAM内进行交互，待得到对应的结果后再进行输出。总之，tiling分块计算使

得我们可以用一个CUDA kernel来执行注意力的所有操作。从HBM中加载输入数据，在SRAM中执行所

有的计算操作(矩阵乘法、mask、softmax、dropout、矩阵乘法)，再将计算结果写回到HBM中。​

Q, K, V
O
S, P

上图右面表示分块后的状态，由于分块使得SRAM能存储下所有中间态，因而可以在SRAM执行完所有

计算操作后才写回到HBM中，降低了访问HBM的次数。具体如何分片参考

https://www.bilibili.com/video/BV1Zz4y1q7FX/，​

对于float32和bfloat16来说，当 x≥89 时，就会变得很大甚至变成inf，故为了避免发生数值溢出的问

题，需要对指数项减去最大值​
，​
m(x)

​
m(x) :=
​x
​,
f(x) :=
​
,
ℓ(x) :=
​f(x)
​,
softmax(x) :=
​
i
max
i
[ ex
​−m(x)
1
…
ex
​−m(x)
B
]

i
∑
i
ℓ(x)
f(x)(2)

对于分块计算，矩阵乘法和逐点操作(scale，mask，dropout)的分块计算是容易实现的。需要注

## 意的是，在进行softmax分块计算时，需要完整的一行作为输入数据，因为其分母需要对完整一行求

## 和。当我们将输入进行分片后，无法对完整的行数据执行Softmax操作。我们可以通过如下所示方法

## 来获得与完整行Softmax相同的结果，而无需使用近似操作。​

## ​
​

m(x) = m
x
= max
m x
, m x
,
f(x) =
​
,
([
(1)
(2)])
(
(1))
(
(2)))
[ e
f x
m x
−m(x)
( (1))
e
f x
m x
−m(x)
( (2))
( (2)) ]

ℓ(x) = ℓ
x
= e
ℓ
x
+ e
ℓ
x
,
softmax(x) =
​.
([
(1)
(2)])
m x
−m(x)
( (1))
(
(1))
m x
−m(x)
( (2))
(
(2))
ℓ(x)
f(x)
(3

原理：参考https://www.zhihu.com/question/611236756/answer/3132304304​

考虑一个向量​
，将其分为两块​
。采用分块计算，首先计算​
，​
x ∈R2d
x = [x
, x
]
(1)
(2)
x1

由此计算得到的​
并不是子向量​
的结果，因为（10）中的最大值应该是整个向量的

最大值，（11）中的求和项应该是整个向量的求和值。​

softmax(x
)
(1)
x(1)

处理完​
后，显存中只保留​
和​
，节省内存开销，此外保存两个全局标量​
和

​
，分别表示当前最大值和全局exp的求和项，暂时分别等于​
和​
。对于​
采用同

样方式处理：

x(1)
m(x
)
(1)
l(x
)
(1)
m
​
max
l
​
all
m(x
)
(1)
l(x
)
(1)
x(2)

​
也是局部的,利用​
更新​
和​
：​
softmax(x
)
(2)
x(2)
m
​
max
l
​
all

为了消除局部性，就需要利用到新的​
和​
。对于
，需要将其减去的​
替换成

​
，为此做如下变化：​

m
​
max
l
​
all
l(x
)
(2)
m(x
)
(2)

m
​
max

总之，当需要把某个更新为“全局的”时，只要将其乘以一个项：​
，其中 m表示当前l对应的

最大值，​
表示当前最大值。同理也对​
去除局部性。为了去除
的局部性，也做相同的

变化

em−m
​
max
new

m
​
max
new
l
​
all
f(x
)
(2)

因此，全局的​
就更新成了：​
softmax
(x
)
new
(2)

​
softmax
(x
) =
new
(2)

​ =
l
​
all
new
f
(x
)
new
(2)

​ =
l
​
all
new
f(x
) ⋅e
(2)
m(x
−m
​)
(2)

max
new

​
l
​
all
new
softmax(x
) ⋅l(x
) ⋅e
(2)
m(x
−m
​)
(2)

max
new

将公式中的​
替换成​
就可以对​
的softmax进行更新。上述其实是一个增量计算的过程​
x(2)
x(1)

1. 我们首先计算一个分块的局部softmax值，然后存储起来​

2. 当处理完下一个分块时，可以根据此时的新的全局最大值和全局exp求和项来更新旧的softmax

值，接着再处理下一个分块，然后再更新

3. 当处理完所有分块后，此时的所有分块的softmax值都是“全局的”​

整个过程中不需要保存​
和​
在SRAM中，要知道softmax的矩阵S是​
的，SRAM一次存储

不下，如果每次增量计算都要重复加载之前的​
, 则反而会增加HBM访问次数。​
x(1)
x(2)
N × N
x(1)