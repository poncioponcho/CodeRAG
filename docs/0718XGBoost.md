# 0718XGBoost

## XGBoost

## 参考：b站：贪心学院 讲得超级清楚！

## Bagging VS Boosting

Bagging 是一种并行集成方法，通过在原始数据集上进行多次有放回的抽样生成多个子数据集，训

练多个基模型，最终通过投票或平均的方法结合这些基模型的预测结果。

并行训练多个基模型，每个模型独立工作。

每个基模型使用的数据集是通过有放回抽样得到的不同子集。

使用多个弱分类器是为了防止过拟合。（可以把每个分类器看作一个专家）

Boosting 是一种序列集成方法，通过逐步训练基模型，每个基模型试图纠正其前一个模型的错

误。最终，通过加权投票或加权平均的方法结合这些基模型的预测结果。

串行训练多个基模型，每个模型依赖于前一个模型的结果。

每个基模型使用的数据集是调整过权重的同一数据集。

使用多个弱分类器是为了防止欠拟合。（可以把每个分类器看作一个小学生）

提升树——基于残差的训练

Xgboost的原理

1、如何构造目标函数？

2、目标函数直接优化很难，如何近似？用泰勒级数

3、如何把树的结构引入到目标函数？

4、仍然难以优化，要不要使用贪心算法？

1、如何构造目标函数？

假设已经训练了k棵树，那么对于第i个样本最终的预测值为：

目标函数：损失函数+惩罚项

训练决策树时如何控制树的复杂度？

通过调参的方式控制：

-叶节点个数

-树的深度

-叶节点的值

-……

Additive Training （叠加式训练）的方式训练第k棵树

2、使用泰勒级数近似目标函数

在 XGBoost 中，目标函数的优化是通过泰勒级数展开来实现的。这种方法使得 XGBoost 能够高效地优

化目标函数并处理大规模数据。具体来说，XGBoost 使用了目标函数的二阶泰勒展开（包括一阶和二阶

导数）来近似目标函数，从而进行加速计算和优化。

泰勒级数展开

假设损失函数为 （可以是交叉熵损失、MSE等），对于每一个样本 ，定义预测值为
。在第  轮迭

代中，有：

其中，
 是第  棵树的输出。

目标函数
 对于所有样本的损失可以表示为：

这里，
是正则化项，用于控制模型复杂度。

为了优化这个目标函数，使用二阶泰勒展开，将其展开到二阶导数项。具体地，对于每个样本 ，损失

函数
 在
处的二阶泰勒展开为：

其中：

是一阶导数。

是二阶导数。

优化目标函数

## 由于
是常数项，不影响优化过程，可以将其去掉。于是目标函数变为：

接下来要考虑的问题就是如何把
  、
参数化。

3、如何把树的结构引入到目标函数？

如何去构造一棵树？我们先不考虑这个问题

先假定我们已经有一棵树了，那我们怎么用参数的方式把它表示出来？

我们希望将
 用每个叶节点的权重
 表示出来。假设树结构已经确定，样本
 所属的叶节点为

。因此，模型输出可以表示为：

树的复杂度 = 叶节点个数 + 叶节点权重

XGBoost 的复杂度项（正则化项）通常定义为：

其中：

是叶节点的数量。

是第 个叶节点的权重。

和 是超参数，用于控制正则化强度。

新的目标函数

最终，目标函数可以写成：

## 为了进一步简化和优化这个目标函数，我们对其进行分组。由于每个叶节点  上的权重
 只影响那些

## 落在该叶节点的样本，我们可以将样本按照叶节点进行分组。设
 表示落在叶节点 的样本集合，则目

## 其中：

是叶节点  上所有样本的一阶导数之和。

是叶节点  上所有样本的二阶导数之和。

将这部分目标函数合并，我们得到：

求解叶节点权重

我们对每个叶节点  的权重
 进行优化，使得目标函数最小化。对于每个 ，优化目标是：

对
 求导并设导数为零，得到最优权重
 的闭式解：

解得：

将最优权重
代入目标函数，得到最终的最小化的目标函数值：

4、如何寻找树的形状？贪心

和决策树的构造类似，只不过把熵换成了

1. 计算一阶和二阶导数：对于每个样本，计算损失函数的一阶导数
 和二阶导数
 （已知）。

2. 构建树结构：根据导数信息，构建新的树。选择最佳分裂点，分裂节点以最大化目标函数的增益。

3. 计算叶节点权重：对于每个叶节点，计算其最优的权重
，使得目标函数最小。

4. 更新预测值：将新树的输出添加到当前的预测值中。